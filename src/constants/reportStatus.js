// Shared between ReportsTable.jsx and ReportsList.jsx (search needs the
// exact label shown in the Estado column, not the raw status enum).
export const BADGE_CLASS = { signed: 'badge-signed', completed: 'badge-completed', draft: 'badge-draft' }
export const STATUS_LABEL = { signed: 'Firmado', completed: 'Completado', draft: 'Borrador' }
